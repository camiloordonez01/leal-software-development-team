const { UserStorage } = require('./User')
const { MovementStorage } = require('./Movement')
const { RedeemStorage } = require('./Redeem')

module.exports = { UserStorage, MovementStorage, RedeemStorage }
