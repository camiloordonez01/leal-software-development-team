const { UserRepository } = require('./User')
const { MovementRepository } = require('./Movement')
const { RedeemRepository } = require('./Redeem')

module.exports = { UserRepository, MovementRepository, RedeemRepository }
