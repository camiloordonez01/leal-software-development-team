class MovementEntity {
    constructor({ id, nameUser, points, detail }) {
        this.id = id
        this.nameUser = nameUser
        this.points = points
        this.detail = detail
    }
}

module.exports = { MovementEntity }
