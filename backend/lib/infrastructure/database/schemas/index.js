const { UserSchema } = require('./User')
const { MovementSchema } = require('./Movement')
const { RedeemSchema } = require('./Redeem')

module.exports = {
    UserSchema,
    MovementSchema,
    RedeemSchema,
}
